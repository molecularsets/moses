language: python

python:
  - "3.6"

before_install:
  - pip install flake8
  
install:
  - python setup.py install

script:
  - flake8 moses/metrics/
  - flake8 setup.py
  - flake8 scripts/
  - python scripts/metrics/test.py
